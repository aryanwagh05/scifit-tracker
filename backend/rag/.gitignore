# Generated pipeline artifacts
data/

# Local paper sources should stay local unless explicitly needed
sources/*.pdf

# Python cache
__pycache__/
*.py[cod]

# .env is committed intentionally so teammates can run the project
