# Sources

Store normalized research text inputs used for embedding.

Suggested layout:
- `raw/` for untouched exports
- `clean/` for cleaned markdown or plain text

Do not commit copyrighted full papers unless licensing allows it.
